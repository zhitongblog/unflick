{
  "// note": "Cursor: Settings -> MCP -> Add, or drop this into ~/.cursor/mcp.json (global) or .cursor/mcp.json (project). Needs `unflick` on PATH — the installers put it there.",
  "mcpServers": {
    "unflick": {
      "command": "unflick",
      "args": [
        "--mcp"
      ]
    }
  }
}

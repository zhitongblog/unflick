{
  "// note": "Cursor: Settings -> MCP -> Add, or drop this into ~/.cursor/mcp.json (global) or .cursor/mcp.json (project). The npx form needs no PATH setup.",
  "mcpServers": {
    "unflick": {
      "command": "npx",
      "args": ["-y", "unflick-mcp"]
    }
  }
}

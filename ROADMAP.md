# unflick — 通往主流播放器

> 起点：v0.10.0（2026-08-23）。功能及格线补齐，AI 原生是唯一真领先项，
> 长尾功能、跨平台验证、工程基础都还欠着。

## 论点

我们不可能靠"比 VLC 支持更多格式"赢——那条路已经被走完了。

现实的主流路径是 **IINA 的位置，再往外扩**：做那个"好看的原生播放器"，
顺便是唯一 AI 能真正驱动的。IINA 在 Mac 上成为主流靠的是设计和原生感，
不是功能数量压过 VLC。

由此得出排序原则：

1. **用户会注意到的缺口** > 冷门功能。缩略图预览比 DVD 支持重要一百倍，
   因为前者每次拖进度条都在提醒你"别人有我们没有"。
2. **打磨** > 新功能。第 20 个功能带来的转化，不如把前 19 个做到不硌手。
3. **三平台同等** > 单平台深度。"跨平台"现在只是句话——v0.10 一行都没在
   Mac/Linux 上跑过。
4. **AI 深度**排最后。这块已经领先，且类目尚未被验证；它是楔子，不是地基。

## 衡量标准

不是"做了多少功能"，是这三个问题能不能答"是"：

- 一个 PotPlayer / IINA 老用户装上后，**一小时内会不会因为某个缺失的习惯动作而退回去**？
- 它能不能**当默认播放器**用一个月不出事？
- 三个平台的体验是不是**同一个东西**？

---

## v0.11 — 消除明显缺口

目标：老用户装上后找不到"这个居然没有"。

| # | 功能 | 为什么是它 |
|---|---|---|
| 1 | ~~**进度条缩略图预览**~~ 完成 | 最高频的"别人有我们没有"。IINA / mpv thumbfast / PotPlayer 全有 |
| 2 | ~~**自定义快捷键**~~ 完成 | 老播放器用户的肌肉记忆。PotPlayer 100+ 键可改，我们写死在 switch 里 |
| 3 | ~~**鼠标手势 + 滚轮音量 + 双击全屏 + 中键暂停**~~ 完成 | 不用键盘的人靠这个操作播放器 |
| 4 | ~~**在线字幕搜索（OpenSubtitles）**~~ 完成（GUI 待实机验证） | IINA 内置。我们能生成字幕却不能搜现成的，说不过去 |
| 5 | ~~**最近播放列表**~~ 完成（GUI 待实机验证） | 现在只有 URL 对话框里有历史，主界面没有 |
| 6 | ~~**画面比例 / 旋转 / 缩放 / 去隔行**~~ 完成（GUI 待实机验证） | core 里 `aspect` 已经能调，只差 UI |

**地基 —— 已完成（2026-08-23）**

| # | 内容 | 状态 |
|---|---|---|
| A | **测试进仓库** —— `src-tauri/tests/{playback,understanding}.rs`，34 个集成测试驱动真实二进制 + 真实 libmpv；加上 11 个单元测试，`cargo test` 一条命令 45 个全绿 | 完成 |
| B | **控制端口 / 数据目录可配置** —— `UNFLICK_CONTROL_ADDR`、`UNFLICK_DATA_DIR`。已验证测试不碰用户的 19542 端口和真实 library.db | 完成 |
| C | **GitHub Actions** —— `.github/workflows/ci.yml`，三平台跑测试 + 编译，前端 typecheck | 完成（待首次触发） |
| D | **前端测试运行器** —— vitest，26 个测试覆盖和弦推导与手势阈值。刻意不引入 DOM 环境：这些函数只读普通字段，传普通对象即可 | 完成 |

夹带修正：`pause` / `resume` / `stop` 三条 MCP 描述过于简略（"Pause playback"），
补上了「作用于用户正在看的那个窗口」这个 Phase 0 之后才成立的事实。

**夹带修正（v0.11 #4 时发现）—— settings.json 会被 GUI 抹掉**

`save_settings` 是整块覆写，而前端只序列化它自己认识的字段。结果是
**CLI/MCP 写进去的键（keybindings、mouse_bindings、opensubtitles_api_key）
在用户第一次动 GUI 设置时被静默删掉**。这直接违背 CLI/MCP 优先的契约——
自定义快捷键（v0.11 #2）本身就是这个 bug 的受害者。

改成 `core::settings::merge` 逐键合并，四个单元测试守住。刻意只合并顶层：
深合并会让「删掉一个快捷键」永远做不到，因为缺席的键会被旧值补回来。

## v0.12 — 音频与网络

目标：不只是"看视频的"，且不再局限于本地文件。

| # | 功能 | 状态 |
|---|---|---|
| 1 | **音频均衡器 + 响度归一化**（VLC 用户抱怨最多的缺失项之一） | 完成（GUI 待实机验证）|
| 2 | **变速不变调** —— mpv 默认就开着，但从来没暴露过开关 | 完成 |
| 3 | **Music 模式** —— 封面 + 标签 + 紧凑窗口，音频文件自动进入 | 完成 |
| 4 | **网络路径直接播放** —— 挂载后的共享盘 / UNC，含缓存与错误可见 | 完成（GUI 待实机验证）|
| 5 | **书签** —— 命名时间点，跨会话保留（PotPlayer `P` / `H`） | 完成（GUI 待实机验证）|
| 6 | **播放速度细调** —— 相对调整、任意倍率、GUI 微调 | 完成（GUI 待实机验证）|

**均衡器的实现记录**

10 段 ISO 频点（31Hz–16kHz），每段一个 ffmpeg `equalizer` 滤镜串成 `af` 链，
±12 dB，外加 preamp（`volume`）和响度归一化（`dynaudnorm`）。顺序是
preamp → EQ → normalize：preamp 要在被它让出空间的增益**之前**，normalize
要放最后才能量到用户真正听到的东西。

**没用上 `af-command`**：ffmpeg 的 `equalizer` 把 gain 标成运行时可改，mpv 也有
`af-command <label> <option> <value>`，理论上拖滑块可以不重建滤镜图。**实测在
我们打包的 libmpv 上不工作**——选项名两种写法、带不带 target 参数、
`lavfi=[...]` 和裸 `equalizer=` 两种写法、带前缀和不带前缀的 label，全部返回
"error running command"。`af-command all` 看起来成功，但 `vf-command all` 在
没有任何视频滤镜时同样返回成功，所以那不算证据。结论：每次改动都重建，
代价由 GUI 滑块去抖吸收。

**书签的实现记录**

存在 SQLite（`bookmark` 表），按 `path` 归属，因此 URL 流的书签和断点续播一样
不受解析出的 CDN 地址变化影响。五面对齐：`bookmark add|list|goto|rename|remove|clear`、
六个 MCP 工具、GUI 里进度条上的书签针 + 播放栏弹层（可改名、可删）。
快捷键 `b` 存点、`Shift+B` 开列表。

三个刻意的决定：

- **1 秒内的重复添加算修正，不算第二个位置**。快捷键会被连按、会被长按，
  相差零点几秒的两条不是电影里的两个地方。同时「不带名字的添加」不会抹掉
  已有的名字——否则再按一次键就把标签弄丢了。
- **`bookmark clear` 不会自己猜「全部」**。没有在播放、也没给 `--file` 时直接报错，
  提示可以用 `--all`。猜错的代价是一次性删光。
- **无痕模式下照样写入**。无痕关掉的是自动累积的历史；书签是用户明确要求的东西，
  悄悄丢掉只会像功能坏了。

`bookmark goto` 在同一个文件里是 seek，不是重载——重载会黑一下屏幕并丢掉用户
选好的音轨和字幕轨，只为到达同一个时间点。跨文件才走 `play`，顺带拿到断点保存、
历史记录和 yt-dlp 解析。

**速度细调的实现记录**

`unflick speed` 之前只能设绝对值，且必填。改成与 `subtitle delay` 同构：省略参数是读，
`--relative` 是相对调整（同时加了 `allow_negative_numbers`，否则 `speed -0.1 --relative`
会被 clap 当成选项）。MCP `set_speed` 同步开放 `relative` 与"不传参数即读取"。

两个刻意的决定：

- **相对调整钳位，绝对调整报错**。按住"减速"键应该停在 0.01×，而不是从某一刻起开始
  报错；但手打 `unflick speed 0` 是明确的错误输入，静默改成 0.01× 只会让人以为设成功了。
- **GUI 微调只在 0.25×–4× 之间**，比 mpv 自己的 0.01–100 窄。弹层不是设 40× 的地方，
  跨四个数量级的滑块对"把 1.25× 调成 1.35×"这件事毫无用处。

微调按钮先把当前值对齐到 0.05 网格再步进，否则从 1.25 这样的预设进来会漂成 1.2999999。
快捷键 `Alt+,` / `Alt+.` —— 与倍率阶梯的 `Shift+,` / `Shift+.` 同键不同修饰键。

**网络路径的实现记录**

先说结论：**`smb://` 和 `nfs://` 播不了，而且不是我们能修的。** 向 mpv 问
`protocol-list`，我们打包的这版给出的是 file / http(s) / dav(s) / ftp / sftp /
rtmp* / rtsp / srt / udp / ipfs —— 没有 smb，没有 nfs。ffmpeg 要接 libsmbclient
才有，主流发行版都不带。所以真正能做的是：**挂载之后的路径本来就能播**
（Windows 的 UNC、macOS 的 `/Volumes`、Linux 的 `/mnt`，到 mpv 眼里就是普通文件），
把不能播的那条路说清楚。

于是 `play` 在下发给 mpv 之前先看协议头：认识就放行，不认识就带着**平台对应的**
办法报错——Windows 说去资源管理器映射再用 UNC 路径，macOS 说在 Finder 里连服务器
再走 `/Volumes`，Linux 说 `mount -t cifs`；NFS 单独一套说法，因为它在三个平台上都
不是这么挂的。解析协议头时要求 `://` 且长度 ≥2，否则 `D:\` 会被当成协议。

**顺手修掉一个一直在说谎的地方**：`loadfile` 是异步的、自己从不失败，所以
`unflick play /不存在的文件` 一直返回 `success: true`，`status` 还跟着说
`state: "playing"`。网络共享掉线就是这个 bug 的日常版本。现在 `play` 会等 mpv 的
判决——`FILE_LOADED` 是成功，`END_FILE` 是打不开，10 秒还没消息就返回
`loaded: false`（"还在打开"，不是失败，慢速共享确实会这样）。

两个坑：
- **必须先清空事件队列**。unflick 里没有任何地方读 mpv 的事件（自动连播是轮询
  `eof-reached`），上一个文件留下的 `END_FILE` 还躺在队列里，会被读成"这个文件打不开"。
- **`END_FILE` 要等 `START_FILE` 之后才算数**。在播放中的文件上 `loadfile`，mpv 会
  先给旧文件发 `END_FILE` 再开始新文件——不区分的话，切歌一律报失败。三个集成测试
  踩到过这条，所以它们留在仓库里了。

GUI 侧同步两处：

- `play` 失败以前只 `console.error` 加一个没人接的 `throw`（所有调用方都是
  fire-and-forget）。现在写进 store 的 `openError` 并弹 toast，URL 对话框内联显示且
  不自动关闭——否则点了播放什么都不动，比原来更糟。播放列表的
  next / prev / play_index 同样从 `let _ =` 改成往上报。
- `player_play` 和这三个播放列表命令改成 `#[command(async)]`。Tauri 的同步 command
  跑在主线程上，而 `play` 现在会等最长 10 秒——不改的话，指向一台关机的 NAS 会把
  窗口冻住十秒。

**UNC 路径打开 mpv 缓存**。共享盘在 mpv 眼里是本地文件，缓存默认不开，4K 原盘走
Wi-Fi 每次拖动都卡。只对 Windows UNC 这一种"光看字符串就能确定是网络"的情况打开；
macOS / Linux 的挂载点和本地盘在字符串上无从区分（`/mnt/usb` 不是网络盘），
猜错的代价是让本地播放变慢，所以那两个平台保持 mpv 的默认。

**Music 模式的实现记录**

「Mini 模式」这一格其实早就有了一半：PiP 就是小窗 + 置顶。缺的是**音频**那一半——
放一个 mp3，播放器给你一块黑色矩形和一条进度条，这是道歉的形状。所以做的是 Music
模式：封面、标签、走带，窗口收到 380×560。

**先把窗口形态本身补成五面对齐**。PiP 之前只有一个按钮：脚本能起播、能定位、能截图，
唯独不能把窗口摆成用户要的样子。现在 `WindowMode { Normal, Pip, Music }` 放在
`core::window`，GUI 通过 `WindowHost` trait 接进控制面，于是
`unflick window mode music` / MCP `window_mode` 和按钮走的是同一条路。无头 daemon
的 `window` 是 `None`，它会直说"没有窗口"，而不是假装换好了——和 `play` 以前撒的谎同一类。

**三种模式共用一份状态和一份"回到哪里"**。原来的 `toggle_pip` 是两个进程级 static
加一个写死的 1024×640，谁的窗口不是这个尺寸谁就被改小。两套独立实现会为同一个窗口
打架：在 PiP 里进 Music 再出来，恢复的是错的形状。现在离开 normal 的那一刻记一次几何，
回来时照原样放回去。

**`now_playing`**：`status` 回答机器的问题（路径、位置、状态），Music 模式要的是另一种
——谁唱的、叫什么、有没有画面。`has_video` 特意不等于"有视频轨"：mpv 把内嵌封面也当成
视频轨（同样有 width/height），照抄的话每个带封面的 mp3 都会打开视频布局；真正的判据是
选中轨道上的 `albumart` 标志。封面用 ffmpeg 抽出来（重编码而非 copy——封面是 PNG 的
概率和 JPEG 差不多，统一成一种格式就少一条分支），按路径+大小+mtime 缓存，和进度条
缩略图同一套。给 GUI 的那份额外附带 data URL：webview 读不了磁盘上的任意文件。

**自动进入**是这个功能能不能被用上的关键——没人会去切一个自己没见过的布局。按文件记一次，
所以用户手动退出后不会被拽回去；反向不做（视频文件不会把窗口从 Music 模式拉走）。
可以在设置里关掉。

**实机验证时抓到一个真 bug**：窗口每次进出 Music 模式都会长高 20px，三次就是 60px。
`inner_size()` 读到的和 `set_size()` 吃进去的不是同一个东西。与其写死一个平台常数
（下一台机器就不对），改成设完再读一次、把差值补上一次，只在差值很小时才修——
差得多说明是还没生效的旧值，照着改会把窗口挪到没人要的地方。修完 Music 模式
终于落在设计的 380×560（此前是 380×580），PiP 也终于是 400×250。

**夹带修正 —— CLI 不接受负数**

`unflick subtitle delay -0.5` 一直是报错的（clap 把 `-0.5` 当成选项），而
README 里正拿 `subtitle delay -0.3` 当示例。`audio delay`、`seek` 同样中招。
`filter set` 之前被单独打过补丁，说明这个坑踩过一次但没扫干净。这次三处一起加了
`allow_negative_numbers`。

## 实机验证批次（2026-08-27）

把积压的「GUI 待实机验证」一次跑完。方法：起 GUI，用 CLI 从外部驱动它，
`PrintWindow` 抓图（`PW_RENDERFULLCONTENT`，窗口被遮挡也能抓），
`PostMessage` 点按钮。抓出六个 bug，都是编译器和 headless 测试看不见的那一类。

**1. CLI/MCP 改的列表到不了窗口**

`unflick bookmark add` 加的书签，进度条上要等重新打开文件才出现；
`unflick playlist add` 加的条目，**打开着的播放列表面板永远不会更新**——面板只在
挂载时 fetch 一次。250ms 的状态轮询扛不动这两个列表（一个在 mpv 之外，一个是整表），
所以它们一直靠「面板挂载」和「文件切换」这两个时机。

修法是加一条缝：`core::events::EventSink`。控制面说哪个列表动了，GUI 转成
`unflick:changed` 事件，前端只重取那一个。粒度故意粗——播放列表和一个文件的书签
都是几十行，做增量同步是在发明一个没人要的协议。无头 daemon 那边是 `None`：没有
窗口可通知。

**2. Windows 上音轨菜单显示 "undefined"**

`PlayerBar` 里给原生菜单自己写了个 `type AudioTrack = { id, label, active }`，
而 `audio_list` 返回的是 `{ id, title, lang, codec, selected }`。`invoke<T>()` 是
不检查的类型断言，所以 TypeScript 一声不吭，每一条音轨都显示 "undefined"，
选中的那条也从来不打勾。React 版菜单（macOS/Linux）自己推导 label，是对的——
两份实现里只有一份对。现在 label 推导和类型都收进 `src/lib/tracks.ts`，
配三个单元测试。

**3. Windows 上均衡器根本打不开**

`{showEqualizer && <Equalizer/>}` 写在 `{!IS_WINDOWS && (...)}` 里面，
而在 Windows 上打开它的是**原生菜单**那一项。于是菜单项把 flag 置真，没有任何东西
渲染它。v0.12 #1 就这样带着一个在主力平台上什么都不做的入口发布了。

**4. 均衡器的十条推子是 Windows 原生蓝**

`-webkit-appearance: slider-vertical` 不只是把滑块立起来，它还强制走**原生渲染**，
`::-webkit-slider-thumb` 一概失效。正下方的 preamp 滑块（紫色圆头）就是它们本该有的
样子。Chromium 121 已经删掉这个关键字，`writing-mode: vertical-lr` 才是正解。

**5. 原生控件在深色面板上是白的**

复选框、`<select>`、滚动条由系统绘制，元素背景怎么设都没用。`color-scheme: dark`
一行解决，`<select>` 再补 `appearance: none` + 自绘箭头 + 品牌色焦点环
（原来是一圈系统蓝，压在紫色面板上）。

**6. CLI 打开的文件，字幕菜单是空的**

mpv 会自动挂同名 sidecar `.srt`——`unflick subtitle list` 看得见、屏幕上也在显示，
但菜单里只有一个打了勾的 "Off"，也没法切轨。前端只在自己的 play 路径上
`refreshSubtitles`；轮询发现文件变了会重取章节、书签、标签，唯独漏了字幕轨。

**已验证通过**：章节刻度、书签针与弹层、播放列表面板（含 repeat/shuffle 状态）、
均衡器（十段 + preamp + 预设 + 归一化 + 变速不变调）、画面几何（旋转实测生效，
CLI 读回 `rotate: 90`）、最近播放、在线字幕的无 key 引导页、速度微调
（点三下 −，1.00× → 0.85×，CLI 读回一致）、Music 模式全套。

> 这条缝是 GUI 耦合的，headless 测试覆盖不到，靠实机验证——记在这里是为了下次
> 改动它时知道回归得手动跑一遍。

## v0.13 — 装了就是默认

目标：能当默认播放器用一个月不出事。

- **升级迁移** —— v0.10 已暴露：Tauri 换了 per-user 安装路径，
  从 0.9.x 升级会留下 500 MB 孤儿目录且没有卸载入口。必须在安装器里处理
- 会话恢复（崩溃/重启后回到原处）
- 启动速度（冷启到出画面的时间，直接决定"要不要设成默认"）
- DVD / ISO
- DLNA 投屏

## v1.0 — 打磨与开放

- 三平台**同等**验证（不是"能编译"，是逐条功能实测 + 截图）
- 插件 / 脚本系统（mpv Lua、IINA JS 都有；这是长尾功能的外包方案）
- 浏览器扩展（IINA 有 Safari/Chrome/Firefox）
- 设计打磨：动效、空状态、错误文案、首次启动引导

## 持续 —— AI 深度（已领先，保持）

VLC 4.0 会用 whisper.cpp 抹掉"本地 AI 字幕"这个卖点。能守住的是
**MCP 原生的理解类工具**——更窄但更深：

- 双语字幕（原文+译文同屏）——SubX 已在做
- 说话人分离
- 基于转录的视频摘要
- 跨库语义搜索（"我库里哪个视频讲过 X"）

---

## 明确不做

- 不追格式数量。libmpv 已经覆盖，再比也赢不了
- 不做付费层、账号、云同步。这与 README 的 charter 冲突
- 不做"AI 推荐内容"。我们是播放器，不是内容平台

## 记账

| 版本 | 状态 |
|---|---|
| v0.10.0 | 已发布构建（Windows 双版本已实测）。Mac/Linux 未验证 |
| v0.11 | 六项全部完成；GUI 侧待实机验证（本机 OpenGL 初始化失败，与改动无关）|
| v0.12 | 六项全部完成。GUI 侧：Music 模式已实机验证（含自动切换、尺寸往返、PiP 回归），均衡器 / 书签 / 在线字幕等仍待实机验证 |

# unflick — 通往主流播放器

> 起点：v0.10.0（2026-08-23）。功能及格线补齐，AI 原生是唯一真领先项，
> 长尾功能、跨平台验证、工程基础都还欠着。

## 论点

我们不可能靠"比 VLC 支持更多格式"赢——那条路已经被走完了。

现实的主流路径是 **IINA 的位置，再往外扩**：做那个"好看的原生播放器"，
顺便是唯一 AI 能真正驱动的。IINA 在 Mac 上成为主流靠的是设计和原生感，
不是功能数量压过 VLC。

由此得出排序原则：

1. **用户会注意到的缺口** > 冷门功能。缩略图预览比 DVD 支持重要一百倍，
   因为前者每次拖进度条都在提醒你"别人有我们没有"。
2. **打磨** > 新功能。第 20 个功能带来的转化，不如把前 19 个做到不硌手。
3. **三平台同等** > 单平台深度。"跨平台"现在只是句话——v0.10 一行都没在
   Mac/Linux 上跑过。
4. **AI 深度**排最后。这块已经领先，且类目尚未被验证；它是楔子，不是地基。

## 衡量标准

不是"做了多少功能"，是这三个问题能不能答"是"：

- 一个 PotPlayer / IINA 老用户装上后，**一小时内会不会因为某个缺失的习惯动作而退回去**？
- 它能不能**当默认播放器**用一个月不出事？
- 三个平台的体验是不是**同一个东西**？

---

## v0.11 — 消除明显缺口

目标：老用户装上后找不到"这个居然没有"。

| # | 功能 | 为什么是它 |
|---|---|---|
| 1 | ~~**进度条缩略图预览**~~ 完成 | 最高频的"别人有我们没有"。IINA / mpv thumbfast / PotPlayer 全有 |
| 2 | ~~**自定义快捷键**~~ 完成 | 老播放器用户的肌肉记忆。PotPlayer 100+ 键可改，我们写死在 switch 里 |
| 3 | ~~**鼠标手势 + 滚轮音量 + 双击全屏 + 中键暂停**~~ 完成 | 不用键盘的人靠这个操作播放器 |
| 4 | ~~**在线字幕搜索（OpenSubtitles）**~~ 完成（GUI 待实机验证） | IINA 内置。我们能生成字幕却不能搜现成的，说不过去 |
| 5 | ~~**最近播放列表**~~ 完成（GUI 待实机验证） | 现在只有 URL 对话框里有历史，主界面没有 |
| 6 | ~~**画面比例 / 旋转 / 缩放 / 去隔行**~~ 完成（GUI 待实机验证） | core 里 `aspect` 已经能调，只差 UI |

**地基 —— 已完成（2026-08-23）**

| # | 内容 | 状态 |
|---|---|---|
| A | **测试进仓库** —— `src-tauri/tests/{playback,understanding}.rs`，34 个集成测试驱动真实二进制 + 真实 libmpv；加上 11 个单元测试，`cargo test` 一条命令 45 个全绿 | 完成 |
| B | **控制端口 / 数据目录可配置** —— `UNFLICK_CONTROL_ADDR`、`UNFLICK_DATA_DIR`。已验证测试不碰用户的 19542 端口和真实 library.db | 完成 |
| C | **GitHub Actions** —— `.github/workflows/ci.yml`，三平台跑测试 + 编译，前端 typecheck | 完成（待首次触发） |
| D | **前端测试运行器** —— vitest，26 个测试覆盖和弦推导与手势阈值。刻意不引入 DOM 环境：这些函数只读普通字段，传普通对象即可 | 完成 |

夹带修正：`pause` / `resume` / `stop` 三条 MCP 描述过于简略（"Pause playback"），
补上了「作用于用户正在看的那个窗口」这个 Phase 0 之后才成立的事实。

**夹带修正（v0.11 #4 时发现）—— settings.json 会被 GUI 抹掉**

`save_settings` 是整块覆写，而前端只序列化它自己认识的字段。结果是
**CLI/MCP 写进去的键（keybindings、mouse_bindings、opensubtitles_api_key）
在用户第一次动 GUI 设置时被静默删掉**。这直接违背 CLI/MCP 优先的契约——
自定义快捷键（v0.11 #2）本身就是这个 bug 的受害者。

改成 `core::settings::merge` 逐键合并，四个单元测试守住。刻意只合并顶层：
深合并会让「删掉一个快捷键」永远做不到，因为缺席的键会被旧值补回来。

## v0.12 — 音频与网络

目标：不只是"看视频的"，且不再局限于本地文件。

| # | 功能 | 状态 |
|---|---|---|
| 1 | **音频均衡器 + 响度归一化**（VLC 用户抱怨最多的缺失项之一） | 完成（GUI 待实机验证）|
| 2 | **变速不变调** —— mpv 默认就开着，但从来没暴露过开关 | 完成 |
| 3 | **Music 模式** —— 封面 + 标签 + 紧凑窗口，音频文件自动进入 | 完成 |
| 4 | **网络路径直接播放** —— 挂载后的共享盘 / UNC，含缓存与错误可见 | 完成（GUI 待实机验证）|
| 5 | **书签** —— 命名时间点，跨会话保留（PotPlayer `P` / `H`） | 完成（GUI 待实机验证）|
| 6 | **播放速度细调** —— 相对调整、任意倍率、GUI 微调 | 完成（GUI 待实机验证）|

**均衡器的实现记录**

10 段 ISO 频点（31Hz–16kHz），每段一个 ffmpeg `equalizer` 滤镜串成 `af` 链，
±12 dB，外加 preamp（`volume`）和响度归一化（`dynaudnorm`）。顺序是
preamp → EQ → normalize：preamp 要在被它让出空间的增益**之前**，normalize
要放最后才能量到用户真正听到的东西。

**没用上 `af-command`**：ffmpeg 的 `equalizer` 把 gain 标成运行时可改，mpv 也有
`af-command <label> <option> <value>`，理论上拖滑块可以不重建滤镜图。**实测在
我们打包的 libmpv 上不工作**——选项名两种写法、带不带 target 参数、
`lavfi=[...]` 和裸 `equalizer=` 两种写法、带前缀和不带前缀的 label，全部返回
"error running command"。`af-command all` 看起来成功，但 `vf-command all` 在
没有任何视频滤镜时同样返回成功，所以那不算证据。结论：每次改动都重建，
代价由 GUI 滑块去抖吸收。

**书签的实现记录**

存在 SQLite（`bookmark` 表），按 `path` 归属，因此 URL 流的书签和断点续播一样
不受解析出的 CDN 地址变化影响。五面对齐：`bookmark add|list|goto|rename|remove|clear`、
六个 MCP 工具、GUI 里进度条上的书签针 + 播放栏弹层（可改名、可删）。
快捷键 `b` 存点、`Shift+B` 开列表。

三个刻意的决定：

- **1 秒内的重复添加算修正，不算第二个位置**。快捷键会被连按、会被长按，
  相差零点几秒的两条不是电影里的两个地方。同时「不带名字的添加」不会抹掉
  已有的名字——否则再按一次键就把标签弄丢了。
- **`bookmark clear` 不会自己猜「全部」**。没有在播放、也没给 `--file` 时直接报错，
  提示可以用 `--all`。猜错的代价是一次性删光。
- **无痕模式下照样写入**。无痕关掉的是自动累积的历史；书签是用户明确要求的东西，
  悄悄丢掉只会像功能坏了。

`bookmark goto` 在同一个文件里是 seek，不是重载——重载会黑一下屏幕并丢掉用户
选好的音轨和字幕轨，只为到达同一个时间点。跨文件才走 `play`，顺带拿到断点保存、
历史记录和 yt-dlp 解析。

**速度细调的实现记录**

`unflick speed` 之前只能设绝对值，且必填。改成与 `subtitle delay` 同构：省略参数是读，
`--relative` 是相对调整（同时加了 `allow_negative_numbers`，否则 `speed -0.1 --relative`
会被 clap 当成选项）。MCP `set_speed` 同步开放 `relative` 与"不传参数即读取"。

两个刻意的决定：

- **相对调整钳位，绝对调整报错**。按住"减速"键应该停在 0.01×，而不是从某一刻起开始
  报错；但手打 `unflick speed 0` 是明确的错误输入，静默改成 0.01× 只会让人以为设成功了。
- **GUI 微调只在 0.25×–4× 之间**，比 mpv 自己的 0.01–100 窄。弹层不是设 40× 的地方，
  跨四个数量级的滑块对"把 1.25× 调成 1.35×"这件事毫无用处。

微调按钮先把当前值对齐到 0.05 网格再步进，否则从 1.25 这样的预设进来会漂成 1.2999999。
快捷键 `Alt+,` / `Alt+.` —— 与倍率阶梯的 `Shift+,` / `Shift+.` 同键不同修饰键。

**网络路径的实现记录**

先说结论：**`smb://` 和 `nfs://` 播不了，而且不是我们能修的。** 向 mpv 问
`protocol-list`，我们打包的这版给出的是 file / http(s) / dav(s) / ftp / sftp /
rtmp* / rtsp / srt / udp / ipfs —— 没有 smb，没有 nfs。ffmpeg 要接 libsmbclient
才有，主流发行版都不带。所以真正能做的是：**挂载之后的路径本来就能播**
（Windows 的 UNC、macOS 的 `/Volumes`、Linux 的 `/mnt`，到 mpv 眼里就是普通文件），
把不能播的那条路说清楚。

于是 `play` 在下发给 mpv 之前先看协议头：认识就放行，不认识就带着**平台对应的**
办法报错——Windows 说去资源管理器映射再用 UNC 路径，macOS 说在 Finder 里连服务器
再走 `/Volumes`，Linux 说 `mount -t cifs`；NFS 单独一套说法，因为它在三个平台上都
不是这么挂的。解析协议头时要求 `://` 且长度 ≥2，否则 `D:\` 会被当成协议。

**顺手修掉一个一直在说谎的地方**：`loadfile` 是异步的、自己从不失败，所以
`unflick play /不存在的文件` 一直返回 `success: true`，`status` 还跟着说
`state: "playing"`。网络共享掉线就是这个 bug 的日常版本。现在 `play` 会等 mpv 的
判决——`FILE_LOADED` 是成功，`END_FILE` 是打不开，10 秒还没消息就返回
`loaded: false`（"还在打开"，不是失败，慢速共享确实会这样）。

两个坑：
- **必须先清空事件队列**。unflick 里没有任何地方读 mpv 的事件（自动连播是轮询
  `eof-reached`），上一个文件留下的 `END_FILE` 还躺在队列里，会被读成"这个文件打不开"。
- **`END_FILE` 要等 `START_FILE` 之后才算数**。在播放中的文件上 `loadfile`，mpv 会
  先给旧文件发 `END_FILE` 再开始新文件——不区分的话，切歌一律报失败。三个集成测试
  踩到过这条，所以它们留在仓库里了。

GUI 侧同步两处：

- `play` 失败以前只 `console.error` 加一个没人接的 `throw`（所有调用方都是
  fire-and-forget）。现在写进 store 的 `openError` 并弹 toast，URL 对话框内联显示且
  不自动关闭——否则点了播放什么都不动，比原来更糟。播放列表的
  next / prev / play_index 同样从 `let _ =` 改成往上报。
- `player_play` 和这三个播放列表命令改成 `#[command(async)]`。Tauri 的同步 command
  跑在主线程上，而 `play` 现在会等最长 10 秒——不改的话，指向一台关机的 NAS 会把
  窗口冻住十秒。

**UNC 路径打开 mpv 缓存**。共享盘在 mpv 眼里是本地文件，缓存默认不开，4K 原盘走
Wi-Fi 每次拖动都卡。只对 Windows UNC 这一种"光看字符串就能确定是网络"的情况打开；
macOS / Linux 的挂载点和本地盘在字符串上无从区分（`/mnt/usb` 不是网络盘），
猜错的代价是让本地播放变慢，所以那两个平台保持 mpv 的默认。

**Music 模式的实现记录**

「Mini 模式」这一格其实早就有了一半：PiP 就是小窗 + 置顶。缺的是**音频**那一半——
放一个 mp3，播放器给你一块黑色矩形和一条进度条，这是道歉的形状。所以做的是 Music
模式：封面、标签、走带，窗口收到 380×560。

**先把窗口形态本身补成五面对齐**。PiP 之前只有一个按钮：脚本能起播、能定位、能截图，
唯独不能把窗口摆成用户要的样子。现在 `WindowMode { Normal, Pip, Music }` 放在
`core::window`，GUI 通过 `WindowHost` trait 接进控制面，于是
`unflick window mode music` / MCP `window_mode` 和按钮走的是同一条路。无头 daemon
的 `window` 是 `None`，它会直说"没有窗口"，而不是假装换好了——和 `play` 以前撒的谎同一类。

**三种模式共用一份状态和一份"回到哪里"**。原来的 `toggle_pip` 是两个进程级 static
加一个写死的 1024×640，谁的窗口不是这个尺寸谁就被改小。两套独立实现会为同一个窗口
打架：在 PiP 里进 Music 再出来，恢复的是错的形状。现在离开 normal 的那一刻记一次几何，
回来时照原样放回去。

**`now_playing`**：`status` 回答机器的问题（路径、位置、状态），Music 模式要的是另一种
——谁唱的、叫什么、有没有画面。`has_video` 特意不等于"有视频轨"：mpv 把内嵌封面也当成
视频轨（同样有 width/height），照抄的话每个带封面的 mp3 都会打开视频布局；真正的判据是
选中轨道上的 `albumart` 标志。封面用 ffmpeg 抽出来（重编码而非 copy——封面是 PNG 的
概率和 JPEG 差不多，统一成一种格式就少一条分支），按路径+大小+mtime 缓存，和进度条
缩略图同一套。给 GUI 的那份额外附带 data URL：webview 读不了磁盘上的任意文件。

**自动进入**是这个功能能不能被用上的关键——没人会去切一个自己没见过的布局。按文件记一次，
所以用户手动退出后不会被拽回去；反向不做（视频文件不会把窗口从 Music 模式拉走）。
可以在设置里关掉。

**实机验证时抓到一个真 bug**：窗口每次进出 Music 模式都会长高 20px，三次就是 60px。
`inner_size()` 读到的和 `set_size()` 吃进去的不是同一个东西。与其写死一个平台常数
（下一台机器就不对），改成设完再读一次、把差值补上一次，只在差值很小时才修——
差得多说明是还没生效的旧值，照着改会把窗口挪到没人要的地方。修完 Music 模式
终于落在设计的 380×560（此前是 380×580），PiP 也终于是 400×250。

**夹带修正 —— CLI 不接受负数**

`unflick subtitle delay -0.5` 一直是报错的（clap 把 `-0.5` 当成选项），而
README 里正拿 `subtitle delay -0.3` 当示例。`audio delay`、`seek` 同样中招。
`filter set` 之前被单独打过补丁，说明这个坑踩过一次但没扫干净。这次三处一起加了
`allow_negative_numbers`。

## 实机验证批次（2026-08-27）

把积压的「GUI 待实机验证」一次跑完。方法：起 GUI，用 CLI 从外部驱动它，
`PrintWindow` 抓图（`PW_RENDERFULLCONTENT`，窗口被遮挡也能抓），
`PostMessage` 点按钮。抓出六个 bug，都是编译器和 headless 测试看不见的那一类。

**1. CLI/MCP 改的列表到不了窗口**

`unflick bookmark add` 加的书签，进度条上要等重新打开文件才出现；
`unflick playlist add` 加的条目，**打开着的播放列表面板永远不会更新**——面板只在
挂载时 fetch 一次。250ms 的状态轮询扛不动这两个列表（一个在 mpv 之外，一个是整表），
所以它们一直靠「面板挂载」和「文件切换」这两个时机。

修法是加一条缝：`core::events::EventSink`。控制面说哪个列表动了，GUI 转成
`unflick:changed` 事件，前端只重取那一个。粒度故意粗——播放列表和一个文件的书签
都是几十行，做增量同步是在发明一个没人要的协议。无头 daemon 那边是 `None`：没有
窗口可通知。

**2. Windows 上音轨菜单显示 "undefined"**

`PlayerBar` 里给原生菜单自己写了个 `type AudioTrack = { id, label, active }`，
而 `audio_list` 返回的是 `{ id, title, lang, codec, selected }`。`invoke<T>()` 是
不检查的类型断言，所以 TypeScript 一声不吭，每一条音轨都显示 "undefined"，
选中的那条也从来不打勾。React 版菜单（macOS/Linux）自己推导 label，是对的——
两份实现里只有一份对。现在 label 推导和类型都收进 `src/lib/tracks.ts`，
配三个单元测试。

**3. Windows 上均衡器根本打不开**

`{showEqualizer && <Equalizer/>}` 写在 `{!IS_WINDOWS && (...)}` 里面，
而在 Windows 上打开它的是**原生菜单**那一项。于是菜单项把 flag 置真，没有任何东西
渲染它。v0.12 #1 就这样带着一个在主力平台上什么都不做的入口发布了。

**4. 均衡器的十条推子是 Windows 原生蓝**

`-webkit-appearance: slider-vertical` 不只是把滑块立起来，它还强制走**原生渲染**，
`::-webkit-slider-thumb` 一概失效。正下方的 preamp 滑块（紫色圆头）就是它们本该有的
样子。Chromium 121 已经删掉这个关键字，`writing-mode: vertical-lr` 才是正解。

**5. 原生控件在深色面板上是白的**

复选框、`<select>`、滚动条由系统绘制，元素背景怎么设都没用。`color-scheme: dark`
一行解决，`<select>` 再补 `appearance: none` + 自绘箭头 + 品牌色焦点环
（原来是一圈系统蓝，压在紫色面板上）。

**6. CLI 打开的文件，字幕菜单是空的**

mpv 会自动挂同名 sidecar `.srt`——`unflick subtitle list` 看得见、屏幕上也在显示，
但菜单里只有一个打了勾的 "Off"，也没法切轨。前端只在自己的 play 路径上
`refreshSubtitles`；轮询发现文件变了会重取章节、书签、标签，唯独漏了字幕轨。

**已验证通过**：章节刻度、书签针与弹层、播放列表面板（含 repeat/shuffle 状态）、
均衡器（十段 + preamp + 预设 + 归一化 + 变速不变调）、画面几何（旋转实测生效，
CLI 读回 `rotate: 90`）、最近播放、在线字幕的无 key 引导页、速度微调
（点三下 −，1.00× → 0.85×，CLI 读回一致）、Music 模式全套。

> 这条缝是 GUI 耦合的，headless 测试覆盖不到，靠实机验证——记在这里是为了下次
> 改动它时知道回归得手动跑一遍。

## 实机验证补记（2026-08-29）

之前挡住一批 GUI 验证的 `LoadLibrary failed with error 87` 随机器重启消失了
（当时用没动过的 v0.10.0 正式版复现过同样的错，确认是显示驱动状态，不是改动）。
补完了欠着的那一项：

**磁盘清理（v0.13 #1）已实机验证**。用 `UNFLICK_LEGACY_DIR` 指到一个假的残留目录
（425,300,000 字节 + `thumbs/` + `covers/`）启动 GUI：设置面板显示「旧版本残留了
405.6 MB」，文案写明缩略图和封面会保留；按下按钮后目录里只剩 `thumbs/` 和
`covers/`，那一段就地变成「已释放 405.6 MB」。**没有碰本机真实的那份残留。**

**顺带验证**：v0.11 #2 自定义快捷键端到端可用——`unflick keybind set settings g`
写进 settings.json，重启 GUI 后按 `g` 确实打开设置面板。（键位表是启动时读的，
改完不重启不生效，这本身合理，但值得记一笔。）

**发现的缺口 —— 设置面板中英文混排**。面板整体是中文，但十几处字符串是硬编码英文：
`SCREENSHOTS` 段的标题、说明和 `Choose...` 按钮，whisper 安装说明，代理段的
`Use system proxy` / `No system proxy is currently set...`，yt-dlp 段的
`✓ Ready — supports ...` / `Click Update to fetch...` / `bundled with unflick`，
字幕语言下的 `Hit Save to apply.`。同一个面板里 74 处走了 `t.`，这些漏了。
非英语用户第一次打开设置就会看到，属于 v1.0「设计打磨」里应该清掉的那类。

**测试环境的两个坑**（下次少走弯路）：脚本 `Start-Process` 起的窗口是最小化的
（`IsIconic`，rect 在 -32000），要先 `ShowWindow(hwnd, 9)`，否则 `PrintWindow`
抓出来是 160×28；面板内滚动用 `WM_MOUSEWHEEL` / PageDown / 拖滚动条**都进不去
Chromium**，直接 `SetWindowPos` 把窗口拉到 1040×1500 让整页装下最省事
（`PrintWindow` 能抓超出屏幕的部分）。

## v0.13 — 装了就是默认

目标：能当默认播放器用一个月不出事。

- ~~**升级迁移**~~ 完成（GUI 已实机验证 2026-08-29）
- ~~**会话恢复**~~ 完成（关窗/崩溃都不再丢进度；首屏「继续观看」）
- ~~**启动速度**~~ 完成（冷启到画面 1520 → 1196 ms；控制端口 3275 → 1358 ms）
- ~~**DVD / ISO**~~ 完成，**已用真实 DVD 镜像端到端验证**（2026-08-29）
- ~~**DLNA 投屏**~~ 完成，**已在真实设备上端到端验证**（小米盒子，2026-08-29）

**升级迁移的实现记录（v0.13 #1）**

先把「500 MB 孤儿目录」查清楚，结论比记的更具体：

- 旧版装在 `%LOCALAPPDATA%\unflick`，新版装在 `%LOCALAPPDATA%\Programs\unflick`
  —— 打包默认值变了，不是谁做的决定。
- **两边写的是同一个注册表键** `HKCU\...\Uninstall\unflick`。升级把旧卸载器的注册
  覆盖了，文件却留在原地：应用列表里没有、快捷方式没有、不主动去翻就永远不会发现。
  这才是「没有卸载入口」的成因。
- 本机实测：孤儿目录 500.8 MB，当前安装 246 MB。

**最容易搞砸的一点**：Windows 上 `dirs_next::cache_dir()` 也是 `%LOCALAPPDATA%`，
所以旧安装目录和**正在使用的缓存目录是同一个路径**——`thumbs/` 和 `covers/` 此刻
就在被当前版本写入。整个文件夹删掉就把它们一起带走了。所以缓存目录是显式点名跳过的，
而且不拿到「这里确实是个安装」的证据（存在 `unflick.exe` 或 `uninstall.exe`，且不是
正在运行的那个）就什么都不删。

**两半都要做**，因为安装器修不了已经升过级的机器：

- `installer-hooks.nsh` 的 `NSIS_HOOK_PREINSTALL`：新文件写入之前先清掉旧 payload。
  刻意**不去跑旧的 `uninstall.exe`**——那会把注册表键一起带走，而那个键此刻已经是
  新版的了。
- `unflick cleanup` / MCP `cleanup`：默认只报告，`--apply` 才删。给已经中招的机器用。

`cleanup` 是唯一**不经过 daemon** 的 MCP 工具：它读写文件，没道理为了 stat 一个目录
去启动播放器；更要紧的是，最需要它的那台机器恰恰是应用起不来的那台。CLI 同理，不调
`ensure_daemon()`。

五个集成测试驱动真实二进制打一个假的安装目录，锁住那条有牙齿的规则：apply 之后
payload 没了，`thumbs/` 和 `covers/` 还在。

GUI 侧在设置面板加了一段：**没有残留就什么都不显示**——一条常驻的「0 B 可清理」正是
这个项目一直在拒绝的设置膨胀。不开终端的人没有别的途径知道这些文件存在。

**启动速度的实现记录（v0.13 #2）**

先能测，再谈快。之前没有任何东西能说出「冷启到画面」是多少：`cargo test` 看不见，
从外面掐表也只能得到一个不可分割的黑箱——控制端口要到最后才应答，所以外部探测
测到的是端口，不是画面。

于是让每个阶段自己报时。`core::boot` 往同一份日志里写 `+NNNms <阶段>`，
`unflick startup` / MCP `startup` 把最近一次启动读回来。**打点是不可关闭的**：
需要开关才有的追踪，恰恰在那次变慢的启动上是关着的。

第一次量出来的结果，和我原先的假设完全不同：

```
+    0ms main: opening a file from the shell
+  756ms setup: entered          ← Tauri 初始化，最大的一块
+  999ms pipeline: ready
+ 1059ms window: shown
+ 1275ms ui: react mounted
+ 1343ms play: mpv reports the file loaded   ← 画面
+ 3091ms control: binding
```

**两个发现**：

1. **画面 1343ms，不是外部测到的 3.1 秒。** 外部探测测的是它自己的连接超时。
2. **窗口已经在放片子了，控制端口还有 1.75 秒没起来。** 这段时间里 `unflick pause`
   或 MCP 调用找不到服务器，会转而拉起一个看不见的 headless daemon —— 正是
   Phase 0 统一控制面要消灭的那个 bug。

`is_daemon_running()` 用的是不带超时的 `TcpStream::connect`。本机上一次被拒绝的
回环连接要 **2 秒**（进程内打点测得，不是猜的）。改成 `connect_timeout(250ms)`：
回环上的服务器要么立刻应答，要么就不在。端口就绪 3275ms → 1358ms。

`ensure_daemon` 一并修了：spawn 的结果原本被 `let _ =` 丢掉，daemon 起不来就变成
一句「daemon not running，请手动运行 unflick daemon」——而 CLI 刚刚自己就试过了。
现在报告真实原因，等待改成按截止时间而不是按次数（20 次 × 100ms 在探测本身要 2 秒
的机器上根本不是 2 秒）。

**去掉主串行**：待播文件在 `main` 里就已知，却要等 WebView 起来、React 挂载、
回调 `consume_pending_file` 才开始加载。现在 mpv 一就绪，Rust 直接通过
`core::daemon::dispatch(ctx, "play", …)` 打开它 —— 走的是 CLI/MCP 同一条 `play`，
不是第二份实现，所以存旧进度、协议检查、断点续播、历史记录一个不少。

**踩的坑**：`pipeline: ready` 只代表渲染线程已启动，渲染上下文是它随后异步建的。
在那之前 loadfile 会直接 END_FILE —— 第一版就这样在 12ms 内报了
「could not open」，而同一个文件三百毫秒后能好好播放。加了
`RenderLoop::wait_ready()` 把这个顺序变成显式的。

前端相应改成**接管**而不是重放：`consume_pending_file` 现在报告后端做了什么
（路径 + 错误），前端 `adoptStartupFile` 只同步 UI 和挂 sidecar 字幕。打不开的
文件会弹出真实原因，不是静默的空窗口。

**顺带修掉一个旧 bug**：mpv 的 `sub-auto` 已经挂了 `movie.srt`，前端又加载一遍，
菜单里每个带字幕的视频都出现两条一模一样的轨道。现在按 `external_file` 去重。
sidecar 查找本身保留 —— 它能找到 mpv 规则漏掉的命名。

**结果**（5 次中位数，release 构建）：

| | 之前 | 之后 |
|---|---|---|
| 到画面 | 1343 / 1520 ms（两次实测） | **1196 ms** |
| 控制端口就绪 | 3275 ms | **1358 ms** |

画面现在**早于 React 挂载**（1196 vs 1286 ms）——旧路径最早也只能从 React 挂载后
再加约 83ms 的 IPC 开始，所以这条串行是被真正拿掉了，不是被挪走了。

**还剩的大头是 Tauri 自身初始化的约 700–1000ms**（插件 + 窗口 + WebView2 环境），
在进入 `setup` 之前，我们的代码一行都还没跑。要动它得换打包层面的东西，不是这次的
范围，但时间线现在会一直把它显示出来。

**会话恢复的实现记录（v0.13 #3）**

先量，结果比「崩溃丢进度」严重：**正常点 X 关窗一样丢**。实测：播放、拖到 0:12、
关窗、重新打开同一个文件 —— 从 0 开始。

原因是断点只在 unflick 自己结束播放时才写：`stop`、切到另一个文件、前端的停止路径。
**没有任何窗口关闭处理**（`grep onCloseRequested` 一无所获），而关窗正是大多数人退出
播放器的方式。崩溃、`taskkill`、断电同理。

**没有再加一个退出钩子。** 进程被杀时钩子不会跑，而「进程能怎么死」这份清单不是能
列全的东西。改成**在位置还成立的时候就写下来**：`core::session` 每 5 秒一跳，把断点
和「正在看什么」一起记下。无论下一刻发生什么，最多丢一跳。

5 秒是这个功能的全部承诺，所以它有一行测试守着——改它就是在改用户会丢多少。

**暂停也记。** 直觉上不该记：暂停是静止的，上一跳已经记过那个位置了。但「跳到
1:20:00、暂停去泡茶、一小时后关窗」什么都没记，恢复时回到最后一次**播放中**的位置，
可能差一个小时。每 5 秒一次 upsert 不值得留这个洞。

**`session` 和 `playback_position` 是两件事**，所以是两张表。后者回答「这个文件再被
打开时从哪开始」——一文件一行，永远保留；前者回答「用户当时在看什么」——同时只有一个，
而且用户一停就不再成立。所以 `stop` 清空会话但**保留断点**：从别处再打开这个文件，
依然回到原处。

`session restore` 直接走 `play`，于是协议检查、断点、历史记录一个不少。文件被删或
移走时明说「不在了，可以 session clear 忘掉它」，而不是把 mpv 的 "could not open"
甩给用户。

GUI 侧在首屏加一张「继续观看」卡：标题、时间戳、进度条，还有一个 ✕。**只有一条**——
一列看了一半的东西是另一个功能，而且是个更差的首屏。它相对下面的「最近播放」多出来的
信息，正是最近播放说不出的那句：这个没看完，看到哪了。

**实机验证抓到一个视觉问题**：进度条第一版只占文字列宽度，而轨道用了 `bg-white/8`
——深色背景上等于不可见，只剩填充的一段，看起来像是给标题加了下划线。轨道改成
`bg-white/15`、3px，才读得出是进度。

**顺带发现（未修）**：首屏中间那一列是 `flex flex-col items-center` 且没有宽度，
被 `unflick` 标题撑成约 190px，所以 `w-full max-w-md` 完全没生效 —— 最近播放列表
也一直是这个窄宽度。属于首屏布局问题，不在会话恢复的范围里，记在这里。

六个集成测试，每一个都**杀进程**而不是干净停止 —— 干净停止走的是本来就好的那条路，
对着改动前的构建也会通过。

**DVD / ISO 的实现记录（v0.13 #4）**

先问 mpv，不先写代码 —— v0.12 的 smb/nfs 就是栽在假设上。完整协议表里
**有 `dvd`、`dvdnav`、`bd`、`bluray`、`br`**，所以这件事能做。

> 顺带修正：v0.12 记录里那份协议清单是**截断的**（只列了十几项）。真实是 50+ 项，
> 还包括 `archive`、`concat`、`edl`、`mf`、`slice`、`subfile`、`webdav`、`mms*`。
> 「没有 smb / nfs」这个结论仍然成立，但那份清单本身不可当参考。

**问题在于 mpv 要被告知这是张碟。** `D:\film.iso` 当成普通路径喂过去，会被当作容器
去解复用然后失败——用户看到的是「unflick 播不了这个」。它要的是协议 + 设备：
`dvd://` 加上 `dvd-device` 指向镜像。

**不用「试一次不行再试另一种」**：拿 `dvd://` 去开蓝光会慢慢地失败——先黑屏再报错，
两种都试就是两倍。碟的结构本身就写着它是什么，所以直接读：`VIDEO_TS` 是 DVD，
`BDMV` 是蓝光，镜像里这两个目录同样找得到。

`core::disc` 里有一个小的 ISO9660 读取器：从 32768 偏移的卷描述符读到主卷描述符，
取根目录的 extent，遍历目录记录找标记目录。纯 UDF（没有 ISO9660 桥）判为蓝光——
BD-ROM 就是 UDF 2.50 且通常不带桥，而 DVD-Video 镜像都带。这是推断，所以 ISO9660
那条路先走。

**最要紧的一条规则**：`.iso` 里装的是别人的备份时，它必须仍然是个普通文件。否则每个
数据镜像都会变成一张播不了的「DVD」。有测试守着。

判断放在 `Player::play` 而不是 daemon 里，因为窗口的拖拽走 `player_play` 不经过
daemon —— 放在 core 的 play 里，三个入口一次都有。

`unflick disc <path>` 能问「这是碟吗、你会怎么开它」而不真的打开。这不只是方便：
**它是这条路由决策唯一能被外部看见的方式**，否则它埋在 play 和 mpv 之间。agent 问
「你能播这个吗」有答案了，测试也有了。

**没做的**：列出碟上有几个标题。查了 `property-list`，这个构建有 `dvd-device`、
`bluray-device`、`dvd-angle`、`bluray-angle`，但**没有 `disc-titles``。硬做就是发一个
查不到数据的接口。标题选择走 mpv 自己的 URL 语法（`dvd://3`），透传已经支持。

**未验证**：真实光盘/镜像的播放。本机没有光驱，也没有可用的 ISO。能验证的部分——
识别、路由、URL 与设备的构造——有 9 个单元测试加 7 个集成测试，其中包括手工构造的
ISO9660 镜像。

---

**DLNA 投屏的实现记录（v0.13 #5）**

三件独立的事，失败方式各不相同，所以分开写：

1. **找到设备** —— SSDP：一个 UDP 组播提问，网络上每台能放视频的电视回一个指向
   自身描述的 URL。
2. **让自己可达** —— 渲染器是**自己来拉**的，所以这台机器要在投屏期间当 HTTP 服务器。
3. **告诉它做什么** —— SOAP 打到渲染器的 AVTransport 服务。

没有引入 XML 库。UPnP 描述很小，要的字段就那么几个且无歧义，按名字取即可；为读四个
标签加一个依赖不划算。

**字节范围不是可选项。** 电视靠 Range 请求定位，很多机型开流就是 `Range: bytes=0-`，
对 200 回应直接拒绝。没有范围支持看起来就是「这个播放器不能拖进度」，或者干脆不起播。

**投屏时暂停本地播放。** 同一部片子差半秒放两遍比只放一个更糟。文件仍然加载着，
所以 `cast stop` 之后有东西可回。

**不缓存发现结果。** `cast to` 重新发现而不是用 `cast list` 找到的那份。发现要几秒，
但记住的那台电视可能已经被拔掉、换了地址、或者变成了邻居家新买的那台——新鲜度比延迟值钱。

**几台电视时不替用户猜。** 只有一台且没指定名字不算歧义；有三台就算，猜错是把片子
放到别人房间的屏幕上。

**测出来一个真 bug**：媒体服务器关连接时发的是 RST 而不是正常关闭。Windows 上，
关闭一个接收缓冲区里还有未读字节的 socket 会发 RST，而 RST 会把客户端还没读完的
响应一起扔掉。表现是测试三次挂一次；在电视上就是偶尔不起播。修法是先
`shutdown(Write)` 说「没有更多了」，再把对方已经发来的读干净才松手。

**未验证**：SSDP 发现本身，需要网络上有设备应答。本机所在网络上没有 DLNA 渲染器
（`cast list` 如实回「no DLNA renderers answered」）。能验证的两半都验了：媒体服务器
用真实 HTTP 请求测（含电视真正会发的那几种 Range、HEAD、416），SOAP 对话对着一个
**会记录被要求做什么的假 AVTransport 服务**测——set URI 的转义、DIDL-Lite 元数据、
Play 的 `<Speed>1</Speed>`、Seek 的 `REL_TIME` 格式、以及渲染器拒绝时它自己的理由
有没有活着传回来。15 个集成测试加 14 个单元测试。

**要用真电视验的是**：`cast list` 能不能找到它，以及找到之后整条链路。前者失败是
SSDP 的事，后者失败会落在已经测过的那些代码上。

**DLNA 真机验证补记（2026-08-29，客厅的小米盒子）**

真机一上来就找不到设备。追下去发现的**不是环境问题，是真 bug**：

```
bind 0.0.0.0        → 0 条应答
bind 192.168.5.7    → 1 条应答  ✓（电视在 192.168.5.12）
bind 192.168.252.1  → 0 条（VMware）
bind 192.168.184.1  → 0 条（VMware）
```

绑 `0.0.0.0` 发组播时，**从哪块网卡出去由路由表决定**，这台机器上它选中了 VMware
的虚拟网卡。任何装了 VMware / VirtualBox / Docker / Hyper-V / VPN 的机器都会中招——
也就是大部分开发机和不少普通机器。症状是「一台电视都搜不到」，看起来像投屏功能坏了，
而不像「问错了方向」。

修法是**每块网卡各开一个 socket 各发一次**，并发跑（串行会让等待时间乘以网卡数）。
发不出组播的网卡（通常是 VPN 隧道）直接跳过，那不是错误，只是不是那一块。

> 期间被 PowerShell 带偏过：同样绑 `0.0.0.0`，.NET 的 `UdpClient` 能收到应答而 Rust
> 收不到，一度让我以为是防火墙。防火墙规则确实也需要（按程序放行入站），但它不是
> 这次的根因。

**真机上验证通过的**：

| 环节 | 结果 |
|---|---|
| 发现 | 找到「客厅的小米盒子」`192.168.5.12:49152` |
| 描述解析 | controlURL 是**不带前导斜杠的相对路径、且含冒号** —— `_urn:schemas-upnp-org:service:AVTransport_control`，解析正确 |
| 媒体服务器 | 电视成功拉流，回读 duration 60.0s |
| 播放 / 暂停 / 继续 | `PLAYING` → `PAUSED_PLAYBACK` → `PLAYING` |
| 拖动 | seek 到 45s，电视回报 45s —— **这条同时证明了 Range 请求是对的** |
| 停止 | 电视回到停止；再遥控报「not casting」 |
| 本地不重复播放 | 投屏后本地停在 8.08s 暂停 |

那台设备的真实描述已经固化成回归测试（含中文设备名和那个怪 controlURL）。

**仍未验证**：DVD / ISO 的真实光盘播放。

**DVD 真机验证补记（2026-08-29）**

拿一个真实的 DVD-Video 镜像（33 MB，Internet Archive 上传者自制，非商业片）测，
一次抓到三件事。

**一、解析器在真实镜像上工作。** 手写的 ISO9660 读取器认出了 `VIDEO_TS`。独立核对了
镜像结构：扇区 16 是主卷描述符、17 是终结符、18–20 是 UDF 识别序列，根目录里
`AUDIO_TS` 和 `VIDEO_TS` 都在。这是一张 ISO9660 + UDF 混合盘 —— 正是代码里先走
ISO9660 那条路的原因。

**二、CLI/MCP 侧播放正常。** `loaded: true`，duration 21.5s 是从 DVD 结构读出来的，
用 mpv 自己截帧拿到了真实解码画面（菜单页 720×540，18 秒处是正片）。

**三、双击 ISO 会让 GUI 崩溃 —— 每次都崩。** `0xC0000409` in `ucrtbase`，mpv 自己的
日志停在半句话「Opening dvd://」。这是只有真机测试能抓到的那类 bug：headless 从来
不崩，编译和 headless 测试全绿。

排查过程（每一步都是测出来的，不是猜的）：

| 假设 | 实验 | 结果 |
|---|---|---|
| stderr 无效 | 用 `-RedirectStandardError` 给真实句柄 | 仍崩 |
| 开得太早 | 加 500ms 延时 | 仍崩 |
| 线程不对 | 换到新起的线程 | 仍崩 |
| 事件循环没起来 | 等 `RunEvent::Ready` | 仍崩 |
| 标准句柄无效 | 三个句柄（Win32 + CRT）全指向 `NUL` | 仍崩 |
| **进程没有控制台** | 附加父控制台 | **不崩** |

最后一条还差一步验证：附加父控制台只在从终端启动时管用，**真实的双击没有父控制台**。
用 WMI 起一个真正无控制台的进程复现，确认了这一点。所以修法是：附加不上就自己分配
一个并立刻隐藏窗口。

**分配时机放在真的要开碟的那一刻**，不是启动时——为了 DVD 让每次启动都闪一下控制台
窗口是不能接受的。普通视频播放实测没有 conhost 子进程，一分钱不付。

> 期间为了看清 mpv 在干什么，加了 `UNFLICK_MPV_LOG` —— 指向一个路径就能拿到 libmpv
> 的完整 verbose 日志。这次全靠它才知道死在「Opening dvd://」。留下了。

**仍未验证**：真实光驱里的物理光盘（本机无光驱），以及蓝光。

## v1.0 — 打磨与开放

- 三平台**同等**验证（不是"能编译"，是逐条功能实测 + 截图）
- 插件 / 脚本系统（mpv Lua、IINA JS 都有；这是长尾功能的外包方案）
- 浏览器扩展（IINA 有 Safari/Chrome/Firefox）
- 设计打磨：动效、空状态、错误文案、首次启动引导

## 持续 —— AI 深度（已领先，保持）

VLC 4.0 会用 whisper.cpp 抹掉"本地 AI 字幕"这个卖点。能守住的是
**MCP 原生的理解类工具**——更窄但更深：

- 双语字幕（原文+译文同屏）——SubX 已在做
- 说话人分离
- 基于转录的视频摘要
- 跨库语义搜索（"我库里哪个视频讲过 X"）

---

## 明确不做

- 不追格式数量。libmpv 已经覆盖，再比也赢不了
- 不做付费层、账号、云同步。这与 README 的 charter 冲突
- 不做"AI 推荐内容"。我们是播放器，不是内容平台

## 记账

| 版本 | 状态 |
|---|---|
| v0.10.0 | 已发布构建（Windows 双版本已实测）。Mac/Linux 未验证 |
| v0.11 | 六项全部完成。GUI：#2 自定义快捷键、#4 在线字幕引导页、#5 最近播放、#6 画面几何已实机验证；#1 缩略图预览、#3 鼠标手势仍未实机 |
| v0.12 | 六项全部完成。GUI：Music 模式（含自动切换、尺寸往返、PiP 回归）、均衡器、书签、速度微调已实机验证；#4 网络路径仍未实机 |
| v0.13 | **5/5 完成，全部实机验证**（2026-08-29）。DLNA 在小米盒子上端到端；DVD 用真实镜像端到端。只剩物理光盘与蓝光未验（本机无光驱）|
